// Declaração de variáveis
giez x = 10;
giez y = 20;
iderennon resultado = pirta;

lo (x < y) {
     resultado = dreje; // verdadeiro
} daor {
     resultado = pirta; // falso
}

bardugon(resultado);
